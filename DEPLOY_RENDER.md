# 🚀 Деплой на Render.com

## Пошаговая инструкция

### 1. Подготовка репозитория

Убедитесь, что ваш проект на GitHub/GitLab:

```bash
# Инициализация Git (если еще не сделано)
git init
git add .
git commit -m "Initial commit"

# Загрузка на GitHub
git remote add origin https://github.com/ваш-username/ai-callcenter.git
git push -u origin main
```

### 2. Создание сервиса на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите **New +** → **Web Service**
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий `ai-callcenter`

### 3. Конфигурация сервиса

**Автоматическая (рекомендуется):**
- Render автоматически обнаружит `render.yaml` и применит настройки

**Ручная настройка:**
- **Name**: `ai-callcenter`
- **Runtime**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python main.py`
- **Plan**: `Starter` (бесплатный) или `Standard` (платный, для продакшена)

### 4. Настройка переменных окружения

В разделе **Environment** добавьте:

```
MISTRAL_API_KEY=ваш_ключ_от_mistral_ai
```

**Получить API ключ:**
1. Зарегистрируйтесь на [console.mistral.ai](https://console.mistral.ai)
2. Создайте новый API ключ
3. Скопируйте и вставьте в Render

### 5. Настройка хранилища (для кэша моделей)

**Важно для Faster-Whisper и Silero!**

1. В настройках сервиса перейдите в **Disks**
2. Нажмите **Add Disk**
3. Настройте:
   - **Name**: `models-cache`
   - **Mount Path**: `/opt/render/.cache`
   - **Size**: `2 GB` (минимум)

Это сохранит загруженные модели между деплоями.

### 6. Деплой

1. Нажмите **Create Web Service**
2. Дождитесь завершения сборки (5-10 минут первый раз)
3. Ваше приложение будет доступно по адресу: `https://your-app-name.onrender.com`

### 7. Проверка работоспособности

После деплоя:

1. Откройте `https://your-app-name.onrender.com`
2. Проверьте статусы: Whisper ✅, Mistral ✅, Silero ✅
3. Попробуйте отправить текстовое сообщение
4. Попробуйте записать голос (работает только на HTTPS!)

## ⚠️ Важные моменты

### Бесплатный план (Starter)

**Ограничения:**
- Сервис "засыпает" после 15 минут неактивности
- Первый запрос после сна займет 30-60 секунд
- 750 часов бесплатно в месяц
- Автоматически выключается при превышении лимита

**Решение:**
- Используйте платный план `Standard` ($7/мес) для production
- Или настройте Cron job для "пробуждения" каждые 10-14 минут

### Производительность

**CPU модели (на Render):**
- Whisper `base`: ~2-5 сек на запрос
- Silero TTS: ~1-2 сек на ответ
- Общее время ответа: 5-10 сек

**Оптимизация:**
- Используйте `tiny` модель Whisper для скорости
- Ограничьте длину ответов AI (параметр `max_tokens`)

### HTTPS для микрофона

Render автоматически выдает HTTPS сертификат.
Микрофон будет работать из коробки!

## 🔧 Troubleshooting

### Сервис не запускается

**Проверьте логи:**
1. В Render перейдите в раздел **Logs**
2. Найдите ошибки в процессе установки

**Частые проблемы:**
- ❌ Не указан `MISTRAL_API_KEY` → Добавьте в Environment
- ❌ Недостаточно RAM → Используйте модель Whisper `tiny` вместо `base`
- ❌ Timeout при установке → Уменьшите версии пакетов в requirements.txt

### Медленная работа

```python
# main.py - измените модель Whisper
whisper_model = WhisperModel("tiny", device="cpu", compute_type="int8")  # Вместо "base"
```

### Ошибка "Out of Memory"

1. Upgrade план на `Standard Plus` (больше RAM)
2. Или уменьшите модель Whisper до `tiny`

### Модели не сохраняются

Проверьте настройку Disk:
- Mount Path должен быть `/opt/render/.cache`
- Размер минимум 2 GB

## 📊 Мониторинг

### Встроенная панель Render

- **Metrics**: CPU, Memory, Network usage
- **Logs**: Real-time логи приложения
- **Events**: История деплоев и рестартов

### Health Check

Render автоматически проверяет `/api/health` каждые 30 секунд.

## 🔄 Автоматические деплои

При каждом push в GitHub, Render автоматически:
1. Скачивает новый код
2. Устанавливает зависимости
3. Перезапускает сервис

**Отключить автодеплой:**
- Settings → Auto-Deploy → Off

## 💰 Цены

### Free (Starter)
- ✅ 750 часов/месяц
- ✅ HTTPS
- ⚠️ Засыпает после 15 мин
- ❌ Нет кастомного домена

### Standard ($7/мес)
- ✅ Не засыпает
- ✅ Больше RAM/CPU
- ✅ Кастомный домен
- ✅ Приоритетная поддержка

### Standard Plus ($25/мес)
- ✅ Еще больше ресурсов
- ✅ Лучше для production

## 📝 Checklist перед деплоем

- [ ] Репозиторий на GitHub/GitLab
- [ ] Файл `render.yaml` в корне проекта
- [ ] Файл `requirements.txt` актуален
- [ ] `.env.example` создан (без реальных ключей!)
- [ ] `.gitignore` настроен (не коммитить .env!)
- [ ] `MISTRAL_API_KEY` готов
- [ ] README.md обновлен

## 🎉 Готово!

Ваше приложение развернуто и доступно по адресу:
```
https://your-app-name.onrender.com
```

**Поделитесь ссылкой с друзьями! 🚀**
