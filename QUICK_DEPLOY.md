# ⚡ БЫСТРЫЙ ДЕПЛОЙ НА RENDER

## 1️⃣ Получить API ключ Mistral (2 минуты)

1. Открыть: https://console.mistral.ai
2. Sign Up / Login
3. API Keys → Create new key
4. Скопировать ключ

## 2️⃣ Загрузить на GitHub (3 минуты)

```bash
cd D:\telephoneApi

# Инициализация
git init
git add .
git commit -m "Initial commit"

# Создать репозиторий на GitHub.com, потом:
git remote add origin https://github.com/ВАШ_USERNAME/ai-callcenter.git
git branch -M main
git push -u origin main
```

## 3️⃣ Деплой на Render (5 минут)

1. Открыть: https://render.com
2. Sign Up (через GitHub)
3. **New +** → **Web Service**
4. **Connect** ваш репозиторий `ai-callcenter`
5. Настройки (автоматически подтянутся из render.yaml):
   - Name: `ai-callcenter` ✅
   - Build Command: `pip install -r requirements.txt` ✅
   - Start Command: `python main.py` ✅
6. **Environment** → Add:
   ```
   MISTRAL_API_KEY = ваш_ключ_сюда
   ```
7. **Create Web Service**

## 4️⃣ Ждать (5-10 минут)

Render скачает, установит и запустит приложение.

## 5️⃣ Готово! 🎉

Ваше приложение: `https://ai-callcenter-XXXX.onrender.com`

---

## ⚠️ Важно!

**Бесплатный план:**
- Засыпает через 15 минут
- Первый запрос после сна = 30-60 сек загрузка

**Для production:**
- Upgrade на Standard ($7/мес)
- Никогда не засыпает

---

## 🐛 Проблемы?

**Не запускается?**
- Проверь MISTRAL_API_KEY в Environment
- Смотри Logs в Render Dashboard

**Медленно?**
- Это нормально для бесплатного CPU
- Используй платный план для скорости

**Микрофон не работает?**
- Render дает HTTPS автоматически - должно работать
- Проверь разрешения браузера

---

## 📞 Все!

Вот и весь деплой. Просто и быстро.
